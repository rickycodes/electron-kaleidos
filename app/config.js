'use strict'

const config = {
  interactive: true,
  ease: 0.1,
  image: 'img/tree.jpg',
  slices: Math.floor(Math.random()*20+5),
  amount: 400,
  size: 30,
  scene: true
}

module.exports = config
