'use strict'

var qs = module.exports = function (s) {
  return document.querySelector(s)
}

qs.all = function (ctx, s) {
  if (arguments.length === 1) {
    s = ctx
    ctx = document
  }
  return [].slice.call(ctx.querySelectorAll(s)).map(function (e) {
    return e
  })
}
